  <%= simple_form_for @listing do |f| %>
    <%= f.input :name, placeholder: 'Listing name' %>
    <%= f.input :address, placeholder: 'Address' %>
    <%= f.submit "Add this listing", class: "btn btn-flat" %>
  <% end %>
